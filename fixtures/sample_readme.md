# Sample Assignment

Write a function `add(a, b)` that returns the sum of two numbers.

## Requirements

- The function must accept two numeric arguments.
- Return the sum as a number.

## Example

```python
assert add(1, 2) == 3
```
